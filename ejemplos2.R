install.packages("gglot2")

library("ggplot2")

numeros <- 1:10;

gglot2(data = numeros)+
  geom_point()+
  labs(title = "Grafico de desmostracion ")
  


print(Data_Autos)

print(Data_Autos$Marca)
print(Data_Autos$Precio)

library(gglot2)
gglot(data = data$Precio)+
  geom_point()+
  labs(title = "Grafico de precios")

print(data$Marca,$precio)